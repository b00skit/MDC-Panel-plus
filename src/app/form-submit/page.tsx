import { Layout } from '@/components/layout/layout';
import { FormSubmitPage } from '@/components/form-submit/form-submit-page';

export default function FormSubmit() {
  return (
    <Layout>
      <FormSubmitPage />
    </Layout>
  );
}
